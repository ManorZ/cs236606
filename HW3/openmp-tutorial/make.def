# for the NVIDIA nvhpc compiler (nvc)
# copy to make.def

CC          = nvc
CLINKER     = $(CC)
OPTFLAGS    = -fast -mp=gpu 
LIBS        =
PRE         = ./

CFLAGS    = $(OPTFLAGS)

OBJ=o
EXE=
RM=rm -f